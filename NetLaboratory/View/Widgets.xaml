<UserControl x:Class="NetLaboratory.View.Widgets"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NetLaboratory.View"
             xmlns:localVm="clr-namespace:NetLaboratory.ViewModel"
             xmlns:popup="clr-namespace:WpfWidgets.popup;assembly=WpfWidgets"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <UserControl.DataContext>
        <localVm:WidegtsViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" ></RowDefinition>
            <RowDefinition Height="Auto" ></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <popup:NotificationBar VerticalAlignment="Center"
                                   MessageQueue="{Binding MessageQueue}">

            </popup:NotificationBar>

            <TextBox Width="100" VerticalAlignment="Center"
                MaxWidth="400"
                materialDesign:HintAssist.Hint="message"
                AcceptsReturn="True"
                Style="{StaticResource MaterialDesignFilledTextBox}"
                TextWrapping="Wrap"
                Text="{Binding SnackBarMessage}"
                />
            <Button Content="Send" Width="80" Height="30" VerticalAlignment="Center"
                    Command="{Binding SendMessageCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
